name: restraintmakerTest
channels:
  - conda-forge
  - rdkit
  - schrodinger
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - codecov
  - flake8

    # Requirements
  - pymol
  - rdkit
  - scipy
  - numpy
  - pandas

    # Pip-only installs
  #- pip:
  #  - codecov


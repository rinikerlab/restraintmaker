name: restraintmakerTest
channels:
  schrodinger
  rdkit
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Requirements
  - pymol
  - rdkit
  - scipy
  - numpy
  - pandas

    # Pip-only installs
  #- pip:
  #  - codecov


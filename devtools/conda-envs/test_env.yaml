name: test
channels:
  - conda-forge
  - rdkit
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - codecov
  - flake8

    # Requirements
  - pymol-open-source
  - rdkit
  - scipy
  - numpy
  - pandas

    # Pip-only installs
  #- pip:
  #  - codecov

